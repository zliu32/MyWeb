import { OnInit, EventEmitter, AfterViewInit, Renderer2 } from '@angular/core';
export declare class NgxCalenderComponent implements OnInit, AfterViewInit {
    private renderer;
    showHeader: boolean;
    clickedDate: EventEmitter<Date>;
    currentDate: string;
    currentMonth: string;
    currentYear: number;
    protected noOfColumn: Array<number>;
    protected noOfRows: Array<number>;
    private currDaysInCurrMonth;
    private count;
    private tdCollection;
    private currMonthFirstDay;
    protected isCurrentDate: boolean;
    private clickedDateCell;
    constructor(renderer: Renderer2);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    getMonthDate: (colInd: number, rowIndex: number) => Number;
    getDate: () => number;
    clickHandler(evt: Event): void;
}
