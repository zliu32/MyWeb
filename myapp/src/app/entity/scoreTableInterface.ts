export interface tagInfo {
    topColor: string,
    circleColor: string,
    buttonColor: string,
    selected: boolean,
    buttonTextColor: string
}

export interface tagInformation {
    focus: boolean,
    backgroundColor: string,
    fontColor: string,
    description: string,
    name: string,
    score: number,
    selected: boolean,
    id: number;
}